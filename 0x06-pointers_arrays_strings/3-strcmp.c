#include <stdio.h>
#include "main.h"

/**
 * _strcmp -  a function that compares two strings
 *
 * @s1: input string
 *
 * @s2: input string
 *
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	char s1[];
	char s2[];
	int n;

	while (n > 0)
	{
		if (s1[] < s2[])
			return (n);
		else if (s1[] < s2[])
			return (-n);
		else if (s1[] == s2[])
			return (0);
	}
	return (n);
}
