#include <stdio.h>
#include "math.h"

/**
 * *_strncpy - a function that copies a string.
 *
 * @n: input character
 *
 * @dest: input string
 *
 * @scr: input string
 *
 * Return: character
 */

char *_strncpy(char *dest, char *src, int n)
{
	int m, j;
	char s1[];
	char s2[];

	j = 0;

	for (m = 0 ; s1[] != '\0' ; m++)
	{
		s2[j] = s1[m];
		j++;
	}
	return (*dest);
}

