#include"main.h"

/**
 * print_sign - A function to print the sign of a number.
 *
 * @n: input number
 *
 * Return: 1 if it's a positive, -1 if it's negative,
 * 0 if it's not
*/

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar(43);
		return (1);
	}
	else if (n < 0)
	{
		_putchar(45);
		return (-1);
	}
	else if
		_putchar(48);
		return (0);
}
